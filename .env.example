# Discord Bot Configuration
DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here

# We remove the GUILD_ID by default to allow the bot to run in multiple servers.
# Currently, no need to use GUILD_ID in environments.
# If you want only to run the bot in a specific server, set the GUILD_ID
# Uncomment and set this if you want to restrict the bot to a single server
# GUILD_ID=your_server_id_here

# Channel IDs (Optional - can be set via slash commands)
# Uncomment and set these if you want to hardcode the channel IDs
# WELCOME_CHANNEL_ID=your_welcome_channel_id_here
# LEAVE_CHANNEL_ID=your_leave_channel_id_here

# Server Configuration (Railway will set this automatically)
PORT=3000

# Node Environment
NODE_ENV=production

# Data Storage Path (Railway Volume mount point)
# For Railway: /app/data
# For local development: leave empty (uses ./data)
DATA_PATH=/app/data